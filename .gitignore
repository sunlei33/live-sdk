node_modules/
dist/
*.log
.DS_Store

# Vite/Vitest 在加载 .ts 配置文件时生成的临时 bundle（正常退出会自删，
# 进程被中断/崩溃时会残留）
*.timestamp-*.mjs

test-results/
playwright-report/
coverage/

# 内部调研/方案文档：只作为本地决策记录，不随仓库与 npm 包分发
# （npm 侧另由 package.json 的 files 白名单限制为 dist，二者互为兜底）
docs/proposal-*.md
